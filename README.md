# Background event counter

Simple key counter to know at the end of the day how many keys we have written pending several updates

### Install

´´´
pip install -r requirements.txt
´´´

### Init Program

´´´
python main.py
´´´
